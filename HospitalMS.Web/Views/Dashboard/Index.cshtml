@* Role-specific dashboards are returned by DashboardController directly as AdminIndex / DoctorIndex / NurseIndex.
   This file is kept as a fallback — the controller never routes here for known roles. *@
@model HospitalMS.Web.Models.DashboardViewModel
@{ Layout = "_Layout"; }
@await Html.PartialAsync("AdminIndex", Model)
