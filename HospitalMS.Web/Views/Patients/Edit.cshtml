@model HospitalMS.Business.Models.UpdatePatientRequest
@{
    ViewData["Title"] = "Edit Patient";
    ViewData["ActivePage"] = "Patients";

    var patientId = ViewContext.RouteData.Values["id"]?.ToString() ?? "";
}

<!-- Page header -->
<div class="page-header">
    <div>
        <div class="pf-breadcrumb">
            <a asp-controller="Dashboard" asp-action="Index" class="pf-breadcrumb-link">Dashboard</a>
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
            <a asp-action="Index" class="pf-breadcrumb-link">Patients</a>
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
            <span class="pf-breadcrumb-current">Edit Patient</span>
        </div>
        <h1 class="page-header-title">Edit Patient</h1>
    </div>
    <a asp-action="Index" class="pf-back-btn">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/>
        </svg>
        Back to Patients
    </a>
</div>

<form asp-action="Edit" method="post" novalidate>
    <div class="row g-4 align-items-start">

        <!-- Main form card -->
        <div class="col-lg-8">
            <div class="dash-card">

                <div class="pf-section-header">
                    <div class="pf-section-icon">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/>
                            <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/>
                        </svg>
                    </div>
                    <div>
                        <div class="pf-section-title">Edit Patient Details</div>
                        <div class="pf-section-subtitle">Update the patient's personal and contact information.</div>
                    </div>
                </div>

                <div class="pf-body">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 small mb-4" role="alert"></div>

                    <!-- First / Last name -->
                    <div class="pf-grid-2 mb-4">
                        <div class="pf-field">
                            <label asp-for="FirstName" class="pf-label">First Name <span class="pf-required">*</span></label>
                            <input asp-for="FirstName" class="pf-input" placeholder="e.g. Jane" />
                            <span asp-validation-for="FirstName" class="pf-error"></span>
                        </div>
                        <div class="pf-field">
                            <label asp-for="LastName" class="pf-label">Last Name <span class="pf-required">*</span></label>
                            <input asp-for="LastName" class="pf-input" placeholder="e.g. Doe" />
                            <span asp-validation-for="LastName" class="pf-error"></span>
                        </div>
                    </div>

                    <!-- DOB / Email -->
                    <div class="pf-grid-2">
                        <div class="pf-field">
                            <label asp-for="DateOfBirth" class="pf-label">Date of Birth <span class="pf-required">*</span></label>
                            <input asp-for="DateOfBirth" type="date" class="pf-input" />
                            <span asp-validation-for="DateOfBirth" class="pf-error"></span>
                        </div>
                        <div class="pf-field">
                            <label asp-for="Email" class="pf-label">Email Address <span class="pf-required">*</span></label>
                            <div class="pf-input-wrap">
                                <span class="pf-input-prefix">
                                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                        <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                                        <polyline points="22,6 12,13 2,6"/>
                                    </svg>
                                </span>
                                <input asp-for="Email" type="email" class="pf-input pf-input--prefixed"
                                       placeholder="patient@example.com" />
                            </div>
                            <span asp-validation-for="Email" class="pf-error"></span>
                        </div>
                    </div>
                </div>

                <div class="pf-footer">
                    <a asp-action="Index" class="pf-btn-cancel">Cancel</a>
                    <button type="submit" class="btn-primary-dash">
                        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                            <polyline points="20 6 9 17 4 12"/>
                        </svg>
                        Save Changes
                    </button>
                </div>
            </div>
        </div>

        <!-- Info sidebar -->
        <div class="col-lg-4 d-flex flex-column gap-3">

            <!-- Danger zone -->
            <div class="dash-card pf-danger-card">
                <div class="pf-tips-header pf-danger-header">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
                        <line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>
                    </svg>
                    Danger Zone
                </div>
                <p class="pf-danger-text">Deleting a patient is permanent and cannot be undone.</p>
                <button type="button" class="pf-btn-delete"
                        onclick="confirmDelete('@patientId', '@(Model.FirstName) @(Model.LastName)')">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <polyline points="3 6 5 6 21 6"/>
                        <path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/>
                        <path d="M10 11v6"/><path d="M14 11v6"/>
                        <path d="M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/>
                    </svg>
                    Delete Patient
                </button>
            </div>

        </div>

    </div>
</form>

<!-- Hidden delete form -->
<form id="deleteForm" method="post" style="display:none">
    @Html.AntiForgeryToken()
</form>

<!-- Delete confirmation modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" style="max-width:400px">
        <div class="modal-content" style="border:none;border-radius:16px;overflow:hidden">
            <div class="modal-body p-4 text-center">
                <div class="delete-modal-icon">
                    <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                         stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <polyline points="3 6 5 6 21 6"/>
                        <path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/>
                    </svg>
                </div>
                <h5 class="mt-3 mb-1" style="font-weight:700;color:#1e293b">Delete Patient</h5>
                <p class="text-muted mb-4" style="font-size:.875rem">
                    Are you sure you want to delete <strong id="deletePatientName"></strong>?
                    This action cannot be undone.
                </p>
                <div class="d-flex gap-2 justify-content-center">
                    <button type="button" class="btn btn-light px-4"
                            style="border-radius:8px;font-weight:600" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" id="confirmDeleteBtn" class="btn btn-danger px-4"
                            style="border-radius:8px;font-weight:600">Delete</button>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    <script>
        let pendingDeleteId = null;
        const deleteModal = new bootstrap.Modal(document.getElementById('deleteModal'));

        function confirmDelete(id, name) {
            pendingDeleteId = id;
            document.getElementById('deletePatientName').textContent = name;
            deleteModal.show();
        }

        document.getElementById('confirmDeleteBtn').addEventListener('click', () => {
            if (!pendingDeleteId) return;
            const form = document.getElementById('deleteForm');
            form.action = `/patients/${pendingDeleteId}/delete`;
            deleteModal.hide();
            form.submit();
        });
    </script>
}
